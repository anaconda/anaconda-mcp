[pytest]
addopts = --html=reports/report.html --self-contained-html
markers =
    stdio_transport: Tests that use STDIO transport as negative-control for KI-011
    regression: Bug regression tests
    slow: Tests with long startup or many iterations (>30 s)
