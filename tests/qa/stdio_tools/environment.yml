name: anaconda-mcp-qa
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.10
  - pytest>=7.0
  - pytest-html>=4.0
  - pytest-timeout>=2.3
  - pytest-metadata>=3.0
# Note: httpx is NOT required — STDIO tests use only stdlib (subprocess, threading, json)
