[pytest]
asyncio_mode = auto
addopts = -p pytest_asyncio --strict-markers
asyncio_default_fixture_loop_scope = function
norecursedirs = tests/qa
markers =
    integration: tests that require a real conda installation and should be skipped in CI
