{
  "manifest_version": "0.4",
  "name": "anaconda-mcp",
  "display_name": "Anaconda MCP",
  "version": "0.1.0",
  "description": "Manage conda environments and packages from Claude Desktop with natural language",
  "long_description": "Anaconda MCP brings conda environment management to Claude Desktop. It uses the bundled `ana` CLI to install and run the Anaconda MCP server, so users do not need to pre-create an `anaconda-mcp` conda environment.\n\n**Requirements:**\n- A supported operating system: macOS Apple Silicon, Linux x86_64/aarch64, or Windows x86_64.\n- Network access on first launch so `ana` can install its managed Anaconda MCP runtime.\n- An Anaconda login and accepted Anaconda MCP Beta Terms before tool calls will succeed.\n\n**What you can do:**\n- List all conda environments\n- Create new conda environments with specific packages\n- Delete/remove conda environments\n- Install packages into environments\n- Delete/remove packages from environments",
  "author": {
    "name": "Anaconda, Inc.",
    "url": "https://anaconda.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/anaconda/anaconda-mcp"
  },
  "homepage": "https://anaconda.com",
  "support": "https://github.com/anaconda/anaconda-mcp/issues",
  "license": "Apache-2.0",
  "server": {
    "type": "node",
    "entry_point": "src/server.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/src/server.js"],
      "env": {}
    }
  },
  "tools": [
    {
      "name": "list_environments",
      "description": "List all installed conda environments"
    },
    {
      "name": "create_environment",
      "description": "Create a new conda environment with optional packages and channels"
    },
    {
      "name": "delete_environment",
      "description": "Delete a conda environment by name or prefix path"
    },
    {
      "name": "remove_environment",
      "description": "Remove a conda environment by name or prefix path (alias for delete)"
    },
    {
      "name": "install_packages",
      "description": "Install packages into a conda environment"
    },
    {
      "name": "delete_packages",
      "description": "Delete packages from a conda environment"
    },
    {
      "name": "remove_packages",
      "description": "Remove packages from a conda environment (alias for delete)"
    }
  ],
  "user_config": {},
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=18"
    }
  },
  "keywords": ["conda", "anaconda", "environment", "packages", "python", "data-science", "mcp"]
}
